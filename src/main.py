from screen_makers.window_screen_maker import WindowScreenMaker
from processors.image_processor import ImageProcessor
from utils import get_windows_names

if __name__ == "__main__":
    window_name = "Norbert — Forecast Data Services Ltd \u200e- OneNote for Windows 10"
    window_screen_maker = WindowScreenMaker(window_name, offsets=(0, 0, 0, 0))
