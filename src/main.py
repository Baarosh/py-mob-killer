from screen_makers.window_screen_maker import WindowScreenMaker
from image.window_screen_maker import WindowScreenMaker
from utils import get_windows_names

if __name__ == "__main__":
    # print(get_windows_names())

    window_screen_maker = WindowScreenMaker(window_name="Norbert — Forecast Data Services Ltd \u200e- OneNote for Windows 10", offsets=(0,0,0,0))
    image_processor = Imagep
    while True:
        screenshot = window_screen_maker.make_screenshot()
        image_processed = image_processor
    print(window_screen_maker.window_size)
